---
version: 1
slug: "frontend-src-app-jsx"
primary_target: "frontend/src/App.jsx"
related_targets: ["frontend/src/styles/site.css","frontend/index.html"]
---

## Direction contract

THESIS: KAVACH presents itself as the artifact it produces — a numbered, auditable incident record — rather than as a product page about video AI. It refuses the category arrangement: dark hero, glowing detection boxes over stock warehouse footage, feature triplet, tilted dashboard mock.

OWN-WORLD: Report stock #F2EFE7 ground, ink #1A1A18, exhibit-stamp red #B23A2E, rule grey #8C8677. A ruled document grid with hairline rules, exhibit numbering, and stamped review status; no cards, no eyebrows. Moving, photographic and terminal material appears only inside mounted figures that carry their own dark ground and a caption bar; the reconstruction plate also carries a pixel scale bar. Three faces, not two: Spectral sets the document, JetBrains Mono is reserved strictly for measured quantities — timecodes, pixel distances, scores — and Archivo carries labels, captions and navigation. The label voice is a real device of the report lineage and was added during the build; it is never the display voice. State reads in weight, rule and texture before colour, so risk survives greyscale. Emptiness is composed, not filled — but that licenses the page field, never the focal exhibit, which fits its frame to the geometry it draws.

STORY: The visitor understands that vision writes the record and language only reads it back; believes it because every score opens into named components and every claim sits beside its stated limit; and opens the demo to click a real event.

FIRST VIEWPORT: A case header block — the case number at document scale with the source geometry, duration and frame rate beneath it — and the primary action "Open the demo" stamped beside it. Below it the one-sentence claim, then the record's own stated finding, labelled and carrying its real figures. The first exhibit plate, the live reconstruction, crosses the fold so the record is visibly already open. AMENDED: the contract originally promised a date in this block. The dataset carries no date field, and adding one would break the fixtures' byte-for-byte reproducibility, so no date is shown and none is invented.

FORM: The Evidence Board. Candidate 1 of my ordered grounded list; seed key d2a7d3ee; presented as IMPECCABLE'S PICK and chosen by the user over assigned index 6, The Review Wall — a user pick beats the roll. Two disciplines donated by the declined hands carry across because they serve the product rather than the assigned card: composed emptiness (from the ikebana hand) and state legible with colour removed (from the industrial-quote hand).

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance
